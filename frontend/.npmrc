; Fail fast when the Node version does not match "engines" in package.json.
; Without this, npm only prints an EBADENGINE warning and installs anyway, and
; the mismatch then shows up as unrelated test failures instead of a clear error.
engine-strict=true
