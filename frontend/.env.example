# Frontend API base URL.
# The app calls endpoints through import.meta.env.VITE_API_URL instead of Vite proxy.
# Example: apiFetch('/auth/login') -> https://localhost:5001/api/auth/login
VITE_API_URL=https://localhost:5001/api