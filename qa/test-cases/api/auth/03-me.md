# QA Test Report - Get Current User Endpoint (Me)

## Feature Information

* **Feature:** Get Current User Info
* **Endpoint:** `GET /api/auth/me`
* **Tester:** Hoang Phuc
* **Test Date:** 2026-06-18
* **Environment:** Local
* **Common Test Data:** User `testuser@example.com` (fullName: `Nguyen Van A`) đã đăng ký. JWT token lấy từ login thành công.

---

## TC-ME-001: Get current user with valid token (happy path)

| Field | Nội dung |
|-------|----------|
| **Test Case ID** | TC-ME-001 |
| **Loại** | Positive |
| **Kỹ thuật** | Happy path |
| **Precondition** | User `testuser@example.com` đã đăng ký và login thành công. Có JWT token hợp lệ. |
| **Test Data** | Header: `Authorization: Bearer <valid_jwt_token>` |
| **Test Steps** | 1. Login with `testuser@example.com` / `Test@1234` to get JWT token <br> 2. Send GET request to `/api/auth/me` with header `Authorization: Bearer <token>` <br> 3. Check response status code <br> 4. Check response body contains user info (id, email, fullName) |
| **Expected Result** | 1. Status 200 OK <br> 2. Response body contains: `{ "id": <number>, "email": "testuser@example.com", "fullName": "Nguyen Van A" }` <br> 3. Response does NOT contain password or passwordHash |
| **Actual Result** | 1. Status 200 OK ✅ <br> 2. Response body contains: `{ "id": <number>, "email": "testuser@example.com", "fullName": "Nguyen Van A" }` ✅ <br> 3. Response does NOT contain password or passwordHash ✅ |
| **Status** | ✅ Pass |
| **Bug link** | — |

---

## TC-ME-002: Access /me without Authorization header (missing token)

| Field | Nội dung |
|-------|----------|
| **Test Case ID** | TC-ME-002 |
| **Loại** | Negative |
| **Kỹ thuật** | Authentication |
| **Precondition** | None |
| **Test Data** | No `Authorization` header sent |
| **Test Steps** | 1. Send GET request to `/api/auth/me` WITHOUT `Authorization` header <br> 2. Check response status code <br> 3. Check response body does NOT contain user info |
| **Expected Result** | 1. Status 401 Unauthorized <br> 2. Response body contains error message: "Unauthorized" or "Token is required" <br> 3. No user info returned |
| **Actual Result** | 1. Status 401 Unauthorized ✅ <br> 2. Response body contains error message: "Unauthorized" ✅ <br> 3. No user info returned ✅ |
| **Status** | ✅ Pass |
| **Bug link** | — |

---

## TC-ME-003: Access /me with invalid/malformed token

| Field | Nội dung |
|-------|----------|
| **Test Case ID** | TC-ME-003 |
| **Loại** | Negative |
| **Kỹ thuật** | Authentication |
| **Precondition** | None |
| **Test Data** | Header: `Authorization: Bearer this-is-not-a-valid-jwt-token` |
| **Test Steps** | 1. Send GET request to `/api/auth/me` with a fake/invalid JWT token <br> 2. Check response status code <br> 3. Check response body does NOT contain user info <br> 4. Verify server does not crash when receiving invalid token |
| **Expected Result** | 1. Status 401 Unauthorized <br> 2. Response body contains error message <br> 3. No user info returned <br> 4. Server does not crash when receiving invalid token |
| **Actual Result** | 1. Status 401 Unauthorized ✅ <br> 2. Response body contains error message ✅ <br> 3. No user info returned ✅ <br> 4. Server does not crash when receiving invalid token ✅ |
| **Status** | ✅ Pass |
| **Bug link** | — |

---

## TC-ME-004: Access /me with expired token

| Field | Nội dung |
|-------|----------|
| **Test Case ID** | TC-ME-004 |
| **Loại** | Negative |
| **Kỹ thuật** | State Transition |
| **Precondition** | Have an JWT token expired > 5min |
| **Test Data** | Header: `Authorization: Bearer <expired_jwt_token>` |
| **Test Steps** | 1. Obtain or create a JWT token that has expired >5min <br> 2. Send GET request to `/api/auth/me` with expired token <br> 3. Check response status code <br> 4. Check response body does NOT contain user info |
| **Expected Result** | 1. Status 401 Unauthorized <br> 2. Response body contains error message: "Token has expired" or "Unauthorized" <br> 3. No user info returned |
| **Actual Result** | 1. Status code: 401 Unauthorized ✅ <br> 2. Response body: `{ "error": "Unauthorized" }` ✅ <br> 3. No user info returned ✅  |
| **Status** | ✅ Pass |
| **Bug link** | — |

---

## TC-ME-005: Verify /me does not return sensitive data

| Field | Nội dung |
|-------|----------|
| **Test Case ID** | TC-ME-005 |
| **Loại** | Positive |
| **Kỹ thuật** | Security |
| **Precondition** | User `testuser@example.com` has registered and logged in successfully. Have valid JWT token. |
| **Test Data** | Header: `Authorization: Bearer <valid_jwt_token>` |
| **Test Steps** | 1. Login to get a valid JWT token <br> 2. Send GET request to `/api/auth/me` with valid token <br> 3. Check response body does NOT contain `password`, `passwordHash`, or any sensitive internal fields <br> 4. Verify response body only contains public fields: `id`, `email`, `fullName`, `createdAt` |
| **Expected Result** | 1. Status 200 OK <br> 2. Response body only contains public info (id, email, fullName) <br> 3. No `password`, `passwordHash`, or internal fields exposed |
| **Actual Result** | 1. Status 200 OK ✅ <br> 2. Response body only contains public info (id, email, fullName) ✅ <br> 3. No `password`, `passwordHash`, or internal fields exposed ✅ |
| **Status** | ✅ Pass |
| **Bug link** | — |

---

## Summary of Results

| Total TCs | Pass | Fail | Blocked | Not Run |
|:---:|:---:|:---:|:---:|:---:|
| 5 | 5 | 0 | 0 | 0 |
